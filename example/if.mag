fun abs(a: Int) -> UInt {
    var b: UInt;

    b = a;
    if (a <= 0)
    {
        if (a == 0)
        {
            b = 26;
        }
        b = b + 5;
    }
    return b;
}

fun main() -> Int {
    return abs(-5);
}
